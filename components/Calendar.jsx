var React = require('react');

var Calendar = React.createClass({
    render: function() {
        return (
            <div className="calendar">

            </div>
        );
    }
});

module.exports = Calendar;
